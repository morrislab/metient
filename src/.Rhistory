tree <- readRDS("~/Desktop/Cornell_Research/Morris_Lab/metient_project/tracerx_nsclc_metsFigures/data/fullTreeOutputList.RDS")
tree
write.csv(tree, "fullTreeOutputList.csv")
type(tree)
class(tree)
tree[0]
write.table(tree, "fullTreeOutputList.txt")
lapply(tree, write, "fullTreeOutputList.txt", append=TRUE)
dput(tree, "fullTreeOutputList.txt")

quit()
